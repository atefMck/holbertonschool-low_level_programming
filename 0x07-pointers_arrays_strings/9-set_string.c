#include "holberton.h"
#include <stdio.h>

/**
* set_string - fills memory with a constant byte.
* @s: the memory area to print in
* @to: the memory area to print in
* Return: void.
*/

void set_string(char **s, char *to)
{
*s = to;
}
