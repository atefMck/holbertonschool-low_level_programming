#include <stdio.h>

/**
 * main - print program name.
 * @argc: number of args
 * @argv: the actual arguments
 * Return: Void.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
