#include <stdio.h>
/**
 * main - Entry point
 * putchar a-z
 * Return: Always 0 (Success)
 */
int main(void)
{
  int a,b;
for (a = '0'; a <= '9'; a++)
{
for (b = '0'; a <= '9'; a++)
{
putchar(a);
putchar(b);
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
