#include <unistd.h>
#include "holberton.h"
/**
 * mul - miltuplies two digits
 * @a: the two int to multiply
 * @b: the two int to multiply
 * Return: result.
 */
int mul(int a, int b)
{
return (a * b);
}
